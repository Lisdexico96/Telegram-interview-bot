This folder contains the older Node.js prototype that was replaced by the Python bot in the project root.

Files here are archived for reference only:
- they are not used by `bot.py`
- they are not used by Railway after the current deploy config changes
- if you ever want to revive this prototype, treat it as a separate app
